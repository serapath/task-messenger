{
  "name": "task-messenger",
  "version": "0.0.1",
  "description": "",
  "main": "src/taskmessenger.js",
  "scripts": {
    "start": "budo web/shim.js:bundle.js --port=10000 --host=localhost --open --live",
    "build": "browserify web/shim.js > bundle.js"
  },
  "devDependencies": {
    "browserify": "^17.0.0",
    "budo": "^11.8.4"
  }
}